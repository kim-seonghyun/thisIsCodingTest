data = input()

result = int(data[0])

for i in range(1, len(data)):
    current = int(data[i])
    if current <= 1 or result <= 1:
        result += current
    else:
        result *= current

print(result)
